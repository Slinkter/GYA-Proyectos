# En tu terminal, dentro del proyecto:

gemini

# Ejemplos de tareas que el orquestador distribuye automáticamente:

# → Activa AGENTE BACKEND

"Crea el módulo auth de NestJS con login JWT"

# → Activa AGENTE FRONTEND

"Crea el componente LoginForm con react-hook-form y zod"

# → Activa AMBOS (backend primero)

"Implementa la feature completa de crear presupuesto"

# → Activa AGENTE BACKEND

"Genera el schema de Prisma completo y la migración inicial"

# → Activa AGENTE BACKEND (SVG)

"Implementa el endpoint POST /svg/generate con Gemini API"

# → Activa AGENTE FRONTEND (SVG)

"Crea el SvgGeneratorPanel con preview y botón de aprobación"

1. docker-compose.yml + .env ← Levantar PostgreSQL local
2. schema.prisma + migración ← Backend: base de datos
3. Auth module NestJS ← Backend: JWT
4. CRUD Presupuestos NestJS ← Backend: core
5. LoginForm + Dashboard ← Frontend: páginas base
6. PresupuestoForm ← Frontend: formulario
7. SVG Generator ← Backend + Frontend: IA
